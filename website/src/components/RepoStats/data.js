// Static fallbacks captured 2026-07-29 from github.com/zio/zio-http.
// Stars and the latest version are refreshed live at runtime (see index.js);
// these values are only shown if the GitHub API is unreachable. Contributors
// has no cheap live count, so it stays static and is bumped here when it drifts.
export const repoStats = {
  owner: 'zio',
  repo: 'zio-http',
  stars: 866,
  contributors: '150+',
  version: 'v3.11.3',
};
